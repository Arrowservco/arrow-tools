# Netlify configuration for BidLens (Next.js App Router).
# The Next.js runtime plugin handles server routes (/api/v1/*) as functions.
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
